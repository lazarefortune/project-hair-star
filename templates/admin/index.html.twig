{% extends 'admin/base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}

    <p class="text-3xl text-gray-700 font-circularStd font-medium mb-4">Salut {{ app.user.fullname }} !</p>

    <section class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 pb-10">
        {% for dashboardCard in dashboardCards %}
            <div class="w-full flex rounded-xl p-8 relative shadow-soft-md bg-white">
                <div class="flex justify-center items-center">
                    <div class="bg-primary-200/50 text-primary-700 p-4 rounded-lg  text-xl flex justify-center items-center">
                        {{ icon(dashboardCard.icon) }}
                    </div>
                </div>

                <div class="ml-7 flex flex-col align-center">
                    <p class="text-2xl my-0">{{ dashboardCard.number }}</p>
                    <p class="text-sm text-gray-400 my-0">{{ dashboardCard.title }}</p>
                </div>
            </div>
        {% endfor %}
    </section>

    <section class="bg-white rounded-xl shadow-soft px-4 py-3 lg:px-8 lg:py-6 mb-10 h-64">
        Section 1
    </section>

    <section class="bg-white rounded-lg shadow-soft px-4 py-3 lg:px-8 lg:py-6 mb-10 h-64">
        Section 2
    </section>

    <section class="bg-white rounded-lg shadow-soft px-4 py-3 lg:px-8 lg:py-6 mb-10 h-64">
        Section 3
    </section>

    <section class="bg-white rounded-lg shadow-soft px-4 py-3 lg:px-8 lg:py-6 mb-10 h-64">
        Section 4
    </section>

{% endblock %}
