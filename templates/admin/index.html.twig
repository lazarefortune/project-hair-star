{% extends 'admin/base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-4">
            <div class="card dashboard__card">
                <div class="card-header">
                    Clients
                </div>
                <div class="card-body">
                    <a href="{{ path('app_admin_clients_index') }}" class="card__number-stats">{{ nbClients }}</a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card dashboard__card">
                <div class="card-header">
                    Réalisations
                </div>
                <div class="card-body">
                    <a href="{{ path('app_admin_realisation_index') }}" class="card__number-stats">{{ nbRealisations }}</a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card dashboard__card">
                <div class="card-header">
                    Réservations
                </div>
                <div class="card-body">
                    <a href="" class="card__number-stats">{{ nbBookings }}</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
