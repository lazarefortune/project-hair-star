{% extends 'admin/base.html.twig' %}

{% block body %}

    <div class="max-w-6xl mx-auto">
        <div class="flex justify-between items-center mb-10">
            <div class="text-gray-700">
                <h1 class="text-xl md:text-3xl font-bold">{% block card_title '' %}</h1>
            </div>

            <div class="text-sm md:text-base md:block">
                {% if block('card_actions') is not empty %}
                    <div class="flex flex-row items-center justify-end">
                        {% block card_actions '' %}
                    </div>
                {% endif %}
            </div>
        </div>

        {#    {% include 'partials/flash.html.twig' %} #}


        <div class="bg-white py-4 px-5 lg:py-5 mb-4 rounded-sm shadow-md">
            {#        {% if block('card_title') is not empty %} #}
            {#            <h1 class="text-lg font-bold mb-4">{% block card_title '' %}</h1> #}
            {#        {% endif %} #}

            {% block card_body '' %}
        </div>
    </div>

{% endblock %}