{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="flex justify-between items-center mb-6">
        <h4 class="text-2xl font-bold md:text-3xl">
            Paramètres
        </h4>
    </div>
    <div class="card flex flex-col md:flex-row md:justify-between md:items-center p-4 md:p-6 mb-6">
        <div class="flex flex-col mb-6">
            <h3 class="text-base sm:text-lg font-semibold">Jours de fermetures</h3>

            <p class="text-sm sm:text-base text-gray-500 font-light">
                Les jours de fermetures sont les jours où le magasin est fermé dans la semaine.
            </p>
        </div>

        <div class="flex justify-center items-center">
            <a href="{{ path('app_admin_shop_settings_offdays') }}">
                Modifier
            </a>
        </div>
    </div>
{% endblock %}

