{% extends 'base_simple.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}

    <div class="page__auth">
        <form method="post" class="card__auth">
            <!-- card header -->
            <div class="card__auth-header">
                <h3 class="card__auth-title">Connexion</h3>
                <p class="card__auth-subtitle">
                    Content de vous revoir!
                </p>
            </div>

{#            <div class="text-center mb-3">#}
{#                <p>#}
{#                    Se connecter avec:#}
{#                </p>#}
{#                <button type="button" class="btn btn-link btn-floating mx-1">#}
{#                    <img src="{{ asset('icons/line-awesome/svg/facebook.svg') }}" alt="facebook" width="30px">#}
{#                </button>#}

{#                <button type="button" class="btn btn-link btn-floating mx-1">#}
{#                    <img src="{{ asset('icons/line-awesome/svg/google.svg') }}" alt="google" width="30px">#}
{#                </button>#}

{#                <button type="button" class="btn btn-link btn-floating mx-1">#}
{#                    <img src="{{ asset('icons/line-awesome/svg/twitter.svg') }}" alt="twitter" width="30px">#}
{#                </button>#}

{#                <button type="button" class="btn btn-link btn-floating mx-1">#}
{#                    <img src="{{ asset('icons/line-awesome/svg/github.svg') }}" alt="github" width="30px">#}
{#                </button>#}
{#            </div>#}

{#            <p class="text-center">ou:</p>#}

            <!-- Error message -->
            {% if error %}
                <div class="alert alert-danger" role="alert">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
            {% endif %}

            <!-- Email input -->
            <div class="form-group mb-4">
                <label for="loginEmail">Email</label>
                <input type="email" id="loginEmail" class="form-control" name="email" value="{{ last_username }}" required autocomplete="email">
            </div>

            <!-- Password input -->
            <div class="form-outline mb-4">
                <label class="form-label" for="loginPassword">Mot de passe</label>
                <input type="password" id="loginPassword" class="form-control" name="password" required autocomplete="current-password">
            </div>

            <!-- 2 column grid layout -->
            <div class="row mb-4">
                <div class="col-md-6 d-flex justify-content-center">
                    <!-- Checkbox -->
                    <div class="form-check mb-3 mb-md-0">
                        <input class="form-check-input" type="checkbox" value="" id="loginCheck" />
                        <label class="form-check-label" for="loginCheck"> Se souvenir de moi </label>
                    </div>
                </div>

                <div class="col-md-6 d-flex justify-content-center">
                    <!-- Simple link -->
                    <a href="">Mot de passe oublié?</a>
                </div>
            </div>

            <input type="hidden" name="_csrf_token"
                   value="{{ csrf_token('authenticate') }}"
            >

            <!-- Submit button -->
            <button type="submit" class="btn btn-primary btn-block mb-4">Se connecter</button>

            <!-- Register buttons -->
            <div class="text-center">
                <p>Pas encore de compte? <a href="{{ path('app_register') }}">S'inscrire</a></p>
            </div>
        </form>
    </div>

{% endblock %}
